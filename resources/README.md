# App icons (Capacitor / App Store)

Place a **1024×1024 PNG** here as `icon.png`, then run:

```bash
npm install -D @capacitor/assets
npx capacitor-assets generate --ios
npx cap sync ios
```

This generates all required iOS sizes and splash screens.
